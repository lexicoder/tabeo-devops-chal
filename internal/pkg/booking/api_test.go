package booking

import (
	//"net/http"
	//"net/http/httptest"
	"testing"
)

func TestBookingCreateHappyPath(t *testing.T) {
	/*
		req, err := http.NewRequest("POST", "/bookings", nil)
		if err != nil {
			t.Error(err)
			return
		}
		req.Header.Add("Content-Type", "application/json")

		rr := httptest.NewRecorder()
		handler := http.HandlerFunc(BookingCreateHandler(nil))
		handler.ServeHTTP(rr, req)

		if status := rr.Code; status != http.StatusCreated {
			t.Errorf("POST /bookings wrong status code: got %d want %d",
				status, http.StatusCreated)
			return
		}
	*/
}
